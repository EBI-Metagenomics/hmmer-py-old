[build-system]
requires = ["setuptools >= 47.1.1", "wheel >= 0.34.2"]
build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
src_paths = ["hmmer"]
known_third_party = ["appdirs", "fasta_reader",
                     "hmmer_reader", "pandas",
                     "pytest", "ray"]
